import { DataType } from "../data-type";
import { GetReferenceType } from "../reference-type";

const getChallengeReference: GetReferenceType = async (data: DataType) => {
  return undefined;
};

export default getChallengeReference;
