import { DataType } from "../data-type";
import { GetReferenceType } from "../reference-type";

const getCollectionReference: GetReferenceType = async (data: DataType) => {
  return undefined;
};

export default getCollectionReference;
