import { DataType } from "../data-type";
import { GetReferenceType } from "../reference-type";

const getPageReference: GetReferenceType = async (data: DataType) => {
  return undefined;
};

export default getPageReference;
