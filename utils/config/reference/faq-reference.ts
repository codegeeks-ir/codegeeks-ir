import { DataType } from "../data-type";
import { GetReferenceType } from "../reference-type";

const getFaqReference: GetReferenceType = async (data: DataType) => {
  return undefined;
};

export default getFaqReference;
