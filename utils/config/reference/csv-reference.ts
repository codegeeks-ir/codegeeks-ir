import { DataType } from "../data-type";
import { GetReferenceType } from "../reference-type";

const getCsvReference: GetReferenceType = async (data: DataType) => {
  return undefined;
};

export default getCsvReference;
